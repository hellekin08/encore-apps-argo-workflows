apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: simple-template-run-
  namespace: cx-encore-di-entra-ns
  labels:
    workflows.argoproj.io/controller-instanceid: argo-workflows-dev
spec:
  serviceAccountName: encore-di-entra
  workflowTemplateRef:
    name: simple-template
  arguments:
    parameters:
      - name: message
        value: "Hello from Workflow!"

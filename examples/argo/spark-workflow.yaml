apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: spark-ewms-run-
  namespace: cx-encore-di-entra-ns
  labels:
    workflows.argoproj.io/controller-instanceid: argo-workflows-dev
spec:
  serviceAccountName: encore-di-entra

  workflowTemplateRef:
    name: spark-template

  arguments:
    parameters:
      - name: load-image
        value: encoreacrdev.azurecr.io/database-to-tmp-storage:TEST-123
